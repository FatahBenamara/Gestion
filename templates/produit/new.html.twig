{% extends 'base.html.twig' %}

{% block title %}New Produit{% endblock %}

{% block body %}
    <h1>Create new Produit</h1>

    {{form_start(form)}}
    {{form_row (form.libelle )}}
    {{form_row (form.pa)}}
    {{form_row (form.pv)}}
    {{form_row (form.tva)}}
    {{form_row (form.stock)}}
    {{form_row (form.image)}}
    {{form_row (form.famille)}}


    <button type="submit">Ajouter Lcommande</button>
    {{form_end(form)}}

    <a href="{{ path('produit_index') }}">back to list</a>
{% endblock %}
