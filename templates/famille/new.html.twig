{% extends 'base.html.twig' %}

{% block title %}New Famille{% endblock %}

{% block body %}
    <h1>Create new Famille</h1>

    {{form_start(form)}}
    {{form_row (form.libelle )}}
   
   {# <button type="submit">Ajouter une famille</button>#}
    {{form_end(form)}}

    <a href="{{ path('famille_index') }}">back to list</a>
{% endblock %}
